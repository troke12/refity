#
# Development only (when running `npm run dev`)
#
VITE_API_URL=http://localhost:5000

#
# Production (Docker): the built frontend uses same-origin `/api/*` and `/v2/*`
# and nginx proxies those paths to the backend.
#
# If you rename your backend service in docker-compose, set this env var on the
# *frontend container* (not here):
#
#   BACKEND_UPSTREAM=backend:5000
#

