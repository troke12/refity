# SFTP/FTP Configuration
FTP_HOST=sftp.example.com
FTP_PORT=22
FTP_USERNAME=sftpuser
FTP_PASSWORD=sftppass
# Optional: path to known_hosts file for SSH host key verification (recommended in production)
# FTP_KNOWN_HOSTS=/etc/refity/known_hosts

# SFTP upload mode: true = sync (file on FTP when push completes), false = async (upload in background)
# SFTP_SYNC_UPLOAD=false

# Production security (required for production)
JWT_SECRET=your-long-random-secret-at-least-32-chars
# Comma-separated allowed origins for CORS (e.g. https://registry.example.com)
# CORS_ORIGINS=https://registry.example.com,https://refity.example.com

# Hetzner Cloud API Configuration (optional - for storage box usage stats)
HCLOUD_TOKEN=your_hetzner_api_token_here
HETZNER_BOX_ID=0

# Server Configuration
PORT=5000

# Frontend: registry URL for "docker pull" (optional; defaults to host in prod, localhost:5000 in dev)
# VITE_REGISTRY_URL=localhost:5000
